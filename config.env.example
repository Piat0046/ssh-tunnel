# ============================================
# SSH 리버스 터널링 설정 파일
# 이 파일을 config.env로 복사하여 사용하세요.
# cp config.env.example config.env
# ============================================

# 퍼블릭 서버 (B) 설정
PUBLIC_HOST="your.public.server.com"
PUBLIC_USER="your_username"
PUBLIC_PORT="22"

# 터널 설정
# B에서 열릴 포트 (A가 이 포트로 접속하면 C로 연결됨)
REMOTE_PORT="22"
# C의 SSH 포트 (이 스크립트를 실행하는 컴퓨터)
LOCAL_PORT="22"

# SSH 키 파일 경로 (비밀번호 없이 자동 접속용)
SSH_KEY="$HOME/.ssh/id_rsa"

# 로그 설정
LOG_DIR="$HOME/Library/Logs"
LOG_FILE="reverse-tunnel.log"
LOG_RETENTION_DAYS=7

# 연결 감지 설정 (초)
# ServerAliveInterval * ServerAliveCountMax = 연결 끊김 감지 시간
SERVER_ALIVE_INTERVAL=10
SERVER_ALIVE_COUNT_MAX=3

# 재연결 대기 시간 (초)
RECONNECT_WAIT=5
